<%- include('partials/header'); -%>

<h1>Hello</h1>
<p><%= startContent %></p>

<% posts.forEach(function(element) { %>
  <h1> <%= element.title %> </h1>
  <p> <%= element.content %> </p>
<% }); %>

<%- include('partials/footer'); -%>
